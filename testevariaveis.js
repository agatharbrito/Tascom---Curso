//escovar os dentes

var pasta = "pasta";
var escova = "escova";
var minutos = 5;

//escovar os dentes

var escovarOsDentes =
  "pegar a " +
  pasta +
  " e a " +
  escova +
  " e escovar os dentes por " +
  minutos +
  "minutos";

var escovar_os_dentes2 = `pegar a ${pasta} e a ${escova} e escovar os dentes por ${minutos} minutos`;

console.log(escovarOsDentes);
